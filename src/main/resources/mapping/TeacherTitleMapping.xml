<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.zcbigdata.mybits_demo.mapper.TeacherTitleMapper" >

<resultMap id="BaseResultMap" type="cn.zcbigdata.mybits_demo.entity.TeacherTitle" >
    <id      column="id"        property="id"        jdbcType="INTEGER" />
    <result  column="sno"  property="sno"  jdbcType="VARCHAR" />
    <result  column="content"  property="content"  jdbcType="VARCHAR" />
    <result  column="status"  property="status"  jdbcType="VARCHAR" />
</resultMap>

<sql id="Base_Column_List" >
    id,sno,content,status
</sql>

<update id="examStudentTitle" parameterType="cn.zcbigdata.mybits_demo.entity.TeacherTitle" >
    update student_titile
    set status = #{status,jdbcType=VARCHAR}
    where id = #{id,jdbcType=VARCHAR}
</update>

<select id="selectStudentTitle" resultMap="BaseResultMap" parameterType="java.lang.Integer">
    select student_title.id,t1.name,t1.sno,student_title.content,student_title.status
    from (select * from student where cno = #{param3})t1 inner join student_title
    where t1.sno = student_title.sno
    limit #{param1},#{param2}
</select>

<select id="selectCount"  resultType="java.lang.Integer" >
    SELECT count(*)
    FROM (select * from student where cno = #{param3})t1 inner join student_title
    where t1.sno = student_title.sno;
</select>

</mapper>