<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.zcbigdata.mybits_demo.mapper.TeacherOpenMapper" >

    <resultMap id="BaseResultMap" type="cn.zcbigdata.mybits_demo.entity.TeacherOpen" >
        <id      column="id"        property="id"        jdbcType="INTEGER" />
        <result  column="sno"  property="sno"  jdbcType="VARCHAR" />
        <result  column="content"  property="content"  jdbcType="VARCHAR" />
        <result  column="status"  property="status"  jdbcType="VARCHAR" />
        <result  column="remark"  property="status"  jdbcType="VARCHAR" />
    </resultMap>

    <sql id="Base_Column_List" >
        id,sno,content,status
    </sql>

    <update id="update" parameterType="cn.zcbigdata.mybits_demo.entity.TeacherOpen" >
        update opening_report
        set status = #{status,jdbcType=VARCHAR}
        where id = #{id,jdbcType=VARCHAR}
    </update>

    <select id="selectAll" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select opening_report.id,t1.name,t1.sno,opening_report.content,opening_report.status,opening_report.remark
        from (select * from student where cno = #{param3})t1 inner join opening_report
        where t1.sno = opening_report.sno
        limit #{param1},#{param2}
    </select>

    <select id="selectCount"  resultType="java.lang.Integer" >
        SELECT count(*)
        FROM (select * from student where cno = #{param3})t1 inner join opening_report
        where t1.sno = opening_report.sno;
    </select>

</mapper>